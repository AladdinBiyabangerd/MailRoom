# Mailroom API — same-origin in prod; Vite proxies /admin/v1 in dev
VITE_ADMIN_API_URL=/admin/v1

# Set to true only for offline UI demo (skips all API calls)
# VITE_DEMO_MODE=true

# Set true to show invite-only copy. Must match ADMIN_INVITE_ONLY_REGISTRATION.
# VITE_INVITE_ONLY_REGISTRATION=true
